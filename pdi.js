
function noenter() {
  return !(window.event && window.event.keyCode === 13);
}

// function( d ) {
//   // 'use strict'
//   var c, children = d.querySelectorAll( '.hide' );
// //    d.querySelector( '#moreChildren' ).reset();
// d.querySelector( '#numberOfChildren' ).addEventListener( 'change',
//       function() {
//         for( c = 0; c < children.length; c ++ ) {
//               children[ c ].classList.add( 'hide' );
//          }
//         for ( c = 0; c < this.value; c ++ ) {
//               children[ c ].classList.remove( 'hide' );
//          }
//       },
//       // false 
//       );
//  }( document );


